```go
/* 循环控制
  for 语句
  无限循环
  select语句 + 监听 context.done

  range关键字遍历数组与切片

  break + label运用（outter）
    （中断 for, switch, select）
  continue + label运用（outter）
  goto语句（不推荐）


/* 变量的作用域
  变量的作用域在其定义的范围之内可以使用。

  在Go语言中，重名的变量的情况不会报错，
  而是在声明局部变量后可以小范围覆盖。

  即在任何作用域都会优先使用作用域范围最小的变量。
```
